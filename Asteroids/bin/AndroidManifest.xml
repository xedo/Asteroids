<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.echedeylima.asteroids"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/TemaAsteroides" >
        <activity
            android:name="com.echedeylima.asteroids.Asteroids"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="AcercaDe"
            android:label="@string/aboutTitle"
            android:theme="@android:style/Theme.Dialog" >
        </activity>
        <activity android:name="Preferencias" android:label="@string/config"></activity>
        <activity android:name="Puntuaciones" android:label="@string/scores"></activity>
        <activity android:name="Juego" android:label="@string/start" android:screenOrientation="landscape" android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
    </application>

</manifest>